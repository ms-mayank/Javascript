/*
[JS Array Method] - Higher order function

//forEach : it iterates through all the value of array - does not returns any value
array.forEach((e)={console.log(e)})

//filter  - It uses to "filter" the data from ; returns new array with filetered datas
a= array.filter((e)=> (e>20))
console.log(a)

//map - the common difference b/w map and foreach is that foreach only iterates and does not returns while map iterates and returns new array

a=array.map(e=>e+1)
console.log(a)

//sort
arr.sort((a,b)=>a-b)

//reduce
The reduce() method returns a single value: the function's accumulated result.

arr = [1, 2, 3, 4];
let b = arr.reduce((total, sum) => (total += sum), 0);
console.log(b);
*/
